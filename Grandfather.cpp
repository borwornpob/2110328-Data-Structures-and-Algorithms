#include <iostream>
#include <algorithm>
#include <unordered_map>

#define ll long long

using namespace std;

int main()
{
    ll n, m;
    cin >> n >> m;

    unordered_map<ll, ll> relations;
    
}